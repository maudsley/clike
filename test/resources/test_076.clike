uint64_t test() {
  matrix_t mat = matrix_t(3, 3);
  for (uint64_t i = 0; i < 3; i++) {
    for (uint64_t j = 0; j < 3; j++) {
      if (i == j) {
        mat[i][j] = 1;
      } else {
        mat[i][j] = 0;
      }
    }
  }
  return mat[1][1];
}
